#!/usr/bin/env bash
# Display count of IP and HTTP code combinations from Apache log

awk 'NF >= 9 { key = $1 " " $9; count[key]++ }
     END {
         for (k in count) {
             print count[k], k
         }
     }' "${1:-access.log}" | sort -rn

